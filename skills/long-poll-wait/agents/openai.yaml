interface:
  display_name: "Long Poll Wait"
  short_description: "Await external jobs with explicit handoffs"
  default_prompt: "Use $long-poll-wait to select the authorized observation path separately from active-turn or scheduled continuation and await this external operation safely."
