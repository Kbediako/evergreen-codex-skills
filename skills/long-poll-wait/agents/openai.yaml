interface:
  display_name: "Long Poll Wait"
  short_description: "Observe external jobs to verified outcomes"
  default_prompt: "Use $long-poll-wait to monitor this external operation until its verified outcome or an explicit handoff."
